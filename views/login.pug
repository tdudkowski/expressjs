extends layout

block content

  form(method="post")
    p
      label Username (1)
      input(type="text" name="username")

    p
      label Password (1)
      input(type="password", name="password")

    p
      input(type="submit", value="Login")